// Command cwl-validate validates a CWL document against the embedded CWL v1.2 schema,
// for use in local development and CI.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "cwl-validate: not yet implemented")
	os.Exit(1)
}
